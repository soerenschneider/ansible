#!/usr/bin/env bash

# https://github.com/soerenschneider/fetcharr
HOST="{{ transmission_webhook_done_url }}"

curl --connect-timeout 5 \
     --max-time 10 \
     --retry 5 \
     --retry-delay 5 \
     --retry-max-time 40 \
     -X POST "${HOST}/webhook"
